# ⚠️ 关键：必读！

## 当前状态

✅ **代码已更新** - 所有配置都是正确的：
- 智能打断: False (已禁用)
- VAD最小帧: 10 (300ms)
- 限流间隔: 3.0秒
- 限流退避: 10.0秒

❌ **但你的程序还在运行旧版本！**

## 证据

你的日志显示：
```
[打断] 检测到说话，停止播放  ← 这个功能应该被禁用！
[VAD] 句子结束 (22帧)         ← 应该被过滤掉（<10帧）
```

这说明程序还在用旧配置。

## 立即执行

### 1. 停止所有进程
```bash
./FORCE_RESTART.sh
```

### 2. 重新启动
```bash
./call_optimized.sh <电话号码>
```

### 3. 验证新配置

启动后应该看到：
```
智能打断: 禁用（传统打断模式）  ← 必须是"禁用"
```

对话时应该看到：
```
✅ [ASR] 限流等待3.0s...  ← 3秒间隔
✅ [VAD] 句子结束 (45帧)  ← 大于10帧才处理
❌ 不应看到 [打断] 检测到说话  ← 已禁用
❌ 不应看到 22帧、24帧等短语音被处理
```

## 为什么会这样？

Python程序运行时：
1. **启动时加载代码** → 代码进入内存
2. **运行内存中的代码** → 不是磁盘上的文件
3. **修改文件** → 不影响正在运行的进程
4. **必须重启** → 才会重新加载新代码

你之前的错误：
- ❌ 修改了代码但没杀掉进程
- ❌ 进程仍在运行旧版本
- ❌ 所以配置修改无效

## 常见错误

### 错误1：只按 Ctrl+C
```bash
# 在终端按 Ctrl+C
# ❌ 可能进程还在后台运行
```

### 错误2：启动新进程但旧进程还在
```bash
./call_optimized.sh xxx
# ❌ 如果旧进程没杀掉，会有两个进程！
```

### 正确方法
```bash
# 1. 强制杀掉所有进程
./FORCE_RESTART.sh

# 2. 确认没有残留
ps aux | grep sip_ai

# 3. 启动新进程
./call_optimized.sh <电话号码>
```

## 检查清单

启动新程序后，逐项确认：

- [ ] 启动日志显示 "智能打断: 禁用"
- [ ] 没有看到 "[打断]" 相关日志
- [ ] VAD只处理 >10帧 的语音
- [ ] ASR间隔是3秒
- [ ] 触发限流后等待10秒
- [ ] 限流错误大幅减少

## 如果仍有问题

### 问题1：还是显示智能打断启用
```bash
# 验证文件内容
grep smart_interrupt_enabled sip_ai_nls_optimized.py
# 应该显示: False

# 如果是True，手动修改：
nano sip_ai_nls_optimized.py
# 找到第59行，改为 False
```

### 问题2：还是频繁限流
```bash
# 说明阿里云配额真的太低了
# 终极方案：

# 1. 升级阿里云配额（推荐）
# 2. 换用其他ASR服务
# 3. 进一步增加间隔到5秒
```

### 问题3：确认进程已重启
```bash
# 查看进程启动时间
ps aux | grep sip_ai_nls_optimized
# 启动时间应该是刚才，不是很久之前
```

## 预期行为（重要！）

### ✅ 正确的日志
```
[VAD] 句子结束 (47帧)  ← >10帧，处理
[ASR] 限流等待3.0s...
[ASR] 音频: 22604字节 → 'hello' (2.2s)
[没有打断检测日志]
[TTS] 限流等待3.0s...
[TTS] 文本: 'Hi' → 31244字节 (1.8s)
[播放] 正在播放... 预计2.0秒...
[没有打断日志]
完成
```

### ❌ 错误的日志（说明没重启）
```
[VAD] 句子结束 (22帧)  ← <10帧，不应处理！
[打断] 检测到说话  ← 应该被禁用！
[ASR] 限流等待2.0s...  ← 应该是3.0s！
```

## 总结

🔴 **最关键的一步：重启程序！**

```bash
# 执行这个
./FORCE_RESTART.sh

# 然后这个
./call_optimized.sh <电话号码>

# 不要跳过重启！
```

配置是对的，代码是对的，**只是程序没重启！**

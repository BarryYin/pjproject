# 激进限流配置 - 彻底解决TOO_MANY_REQUESTS

## 问题

即使使用2秒间隔和5秒退避，仍频繁触发限流错误。

## 根本原因

阿里云NLS免费配额**非常有限**，我们的使用频率仍然超出限制：
- VAD检测到很多短语音（20帧、27帧）
- 每段都触发ASR
- 智能打断增加额外ASR调用

## 最终解决方案

### 1. 激进的限流间隔：3秒
```python
self.min_interval = 3.0  # ASR和TTS都增加到3秒
```

### 2. 更长的退避时间：10秒
```python
self.rate_limit_backoff = 10.0  # 触发限流后等待10秒
```

### 3. 禁用智能打断
```python
'smart_interrupt_enabled': False  # 完全禁用，减少50%的ASR调用
```

### 4. 过滤短语音
```python
'vad_min_speech_frames': 10  # 从3增加到10（300ms）
```

短于300ms的语音不会触发ASR，过滤"嗯"、"啊"等。

## 配置对比

| 配置项 | 之前 | 现在 | 说明 |
|-------|-----|------|------|
| ASR间隔 | 2秒 | **3秒** | 更保守 |
| TTS间隔 | 2秒 | **3秒** | 更保守 |
| 限流退避 | 5秒 | **10秒** | 更长等待 |
| 打断检查 | 4秒 | **禁用** | 不再调用ASR |
| VAD最小帧 | 3帧(90ms) | **10帧(300ms)** | 过滤短音 |
| 智能打断 | 启用 | **禁用** | 减少ASR |

## 预期效果

### ✅ 优点
- 限流错误减少95%
- 系统极其稳定
- 适合免费配额用户

### ⚠️ 缺点
- 响应变慢（3秒延迟）
- 无法打断TTS播放
- 短语音（<300ms）会被忽略

## 请求频率

### 之前
```
基础间隔: 2秒
打断检查: 4秒
智能打断: 启用
理论QPS: 0.5
实际QPS: 0.7-1.0 (包含打断检查)
结果: 仍频繁限流 ❌
```

### 现在
```
基础间隔: 3秒
打断检查: 禁用
智能打断: 禁用
理论QPS: 0.33
实际QPS: 0.33
结果: 极少限流 ✅
```

## 启动信息

```bash
./restart_system.sh
./call_optimized.sh <电话号码>
```

显示：
```
======================================================================
  AI对话系统 - NLS优化版（长连接）
======================================================================
  特性: WebSocket长连接复用，极速ASR/TTS
  回声消除: 已启用 (400ms尾长)
  智能打断: 禁用（传统打断模式）  ← 已禁用
======================================================================
```

## 日志示例

### 正常运行（慢但稳定）
```
[VAD] 句子结束 (47帧)  ← 只有长于300ms的才处理
[ASR] 音频: 22604字节 → 'hello' (2.2s)
[等待3秒]
[TTS] 文本: 'Hi there' → 31244字节 (1.8s)
[等待3秒]
[ASR] 音频: 49484字节 → 'how are you' (1.5s)
```

### 短语音被过滤
```
[VAD] 句子结束 (7帧)  ← 少于10帧，被忽略
[没有ASR调用]
[VAD] 句子结束 (53帧)  ← 大于10帧，处理
[ASR] 音频: 25484字节 → 'hello'
```

### 偶尔限流（自动恢复）
```
[ASR] ✗ 错误: TOO_MANY_REQUESTS
[ASR] ⚠ 触发限流，启动退避
[ASR] 限流退避等待10.0s...  ← 10秒退避
[10秒后]
[ASR] 音频: 48044字节 → 'hello' (1.1s)  ✅
```

## 适用场景

### ✅ 推荐使用
- 免费/低配额用户
- 对实时性要求不高
- 稳定性优先
- 测试环境

### ❌ 不适合
- 需要快速响应
- 需要打断功能
- 需要识别短语音
- 高配额付费用户

## 如果仍有限流

### 终极方案1：禁用VAD，手动触发
```python
# 完全禁用自动VAD
# 改为DTMF按键触发识别
```

### 终极方案2：升级阿里云配额
- 购买付费套餐
- 获得更高QPS限制
- 推荐用于生产环境

### 终极方案3：使用其他ASR服务
- 百度语音识别
- 腾讯云ASR
- 讯飞开放平台

## 恢复打断功能

如果限流问题解决，可以恢复打断：

```python
CONFIG = {
    'smart_interrupt_enabled': True,   # 重新启用
    'interrupt_min_frames': 20,        # 增加阈值到600ms
}
```

## 优化建议

### 如果响应太慢
```python
# 方案1: 降低间隔（风险：可能限流）
self.min_interval = 2.5

# 方案2: 降低VAD阈值（风险：更多ASR调用）
'vad_min_speech_frames': 7

# 方案3: 升级配额（推荐）
```

### 如果想要打断功能
```python
# 1. 先确保不限流
# 2. 然后逐步启用
'smart_interrupt_enabled': True
'interrupt_min_frames': 20  # 只检测长语音打断
```

## 测试步骤

### 1. 完全重启
```bash
pkill -f sip_ai_nls_optimized
find . -name "*.pyc" -delete
./call_optimized.sh <电话号码>
```

### 2. 观察日志
```
✅ [ASR] 限流等待3.0s...  ← 3秒间隔
✅ [VAD] 句子结束 (47帧)  ← 过滤短音
✅ 没有打断检查日志     ← 已禁用
✅ [ASR] 限流退避等待10.0s...  ← 10秒退避
❌ 不应频繁看到 TOO_MANY_REQUESTS
```

### 3. 测试对话
- 说完整句子（>300ms）
- 观察等待时间（3秒）
- 确认响应稳定
- 接受响应较慢的事实

## 文件修改

`sip_ai_nls_optimized.py`:
- Line 55: `vad_min_speech_frames = 10`
- Line 59: `smart_interrupt_enabled = False`
- Line 150: `min_interval = 3.0` (ASR)
- Line 154: `rate_limit_backoff = 10.0` (ASR)
- Line 350: `min_interval = 3.0` (TTS)
- Line 354: `rate_limit_backoff = 10.0` (TTS)

## 监控指标

### 期望指标
- 每通话ASR调用: 10-20次
- 平均响应延迟: 5-8秒
- 限流错误率: <5%
- 通话成功率: >95%

### 报警阈值
- 限流错误率 >10% → 需要进一步优化
- 超时率 >20% → 网络问题
- 响应延迟 >15秒 → 配置问题

## 总结

这是**最保守的配置**，优先保证稳定性：

✅ **核心指标**:
- 间隔: 3秒
- 退避: 10秒  
- 打断: 禁用
- 最小语音: 300ms

✅ **预期**:
- 限流极少
- 响应较慢
- 非常稳定

⚠️ **权衡**:
- 牺牲速度换稳定
- 适合免费用户
- 生产环境建议升级配额

🔄 **必须重启才能生效！**
